# Check whether real_ladybug is importable

Check whether real_ladybug is importable

## Usage

``` r
ladybugdb_is_installed()
```

## Value

`TRUE` if the Python package `real_ladybug` can be imported, `FALSE`
otherwise.

## Examples

``` r
ladybugdb_is_installed()
#> [1] TRUE
```
