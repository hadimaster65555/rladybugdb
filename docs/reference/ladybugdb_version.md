# Return the LadybugDB C library version string

Return the LadybugDB C library version string

## Usage

``` r
ladybugdb_version()
```

## Value

A character string, e.g. `"0.7.0"`.

## Examples

``` r
if (FALSE) { # \dontrun{
ladybugdb_version()
} # }
```
