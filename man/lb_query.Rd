% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/execute.R
\name{lb_query}
\alias{lb_query}
\title{Execute a Cypher query and return a data frame}
\usage{
lb_query(conn, query, parameters = NULL, ...)
}
\arguments{
\item{conn}{An \code{lb_connection} object from \code{\link[=lb_connection]{lb_connection()}}.}

\item{query}{A single character string containing a Cypher query.}

\item{parameters}{A named list of query parameters for parameterised queries.
\code{NULL} (default) means no parameters.}

\item{...}{Passed to \code{\link[=as.data.frame.lb_result]{as.data.frame.lb_result()}}.}
}
\value{
A \code{data.frame}.
}
\description{
Convenience wrapper around \code{\link[=lb_execute]{lb_execute()}} that immediately converts the
result to an R \code{data.frame}. Equivalent to
\code{as.data.frame(lb_execute(conn, query, ...))}.
}
\examples{
\dontrun{
db   <- lb_database(":memory:")
conn <- lb_connection(db)
lb_execute(conn, "CREATE NODE TABLE Person (name STRING, age INT64, PRIMARY KEY(name))")
lb_execute(conn, "CREATE (:Person {name: 'Alice', age: 30})")
df <- lb_query(conn, "MATCH (p:Person) RETURN p.name, p.age")
}

}
